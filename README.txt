================
Hover IP updater
================

Hover IP updater provides a cli (command line interface) to update a IP in
hover. Typical usage often looks like this::

    hover-update --username=USERNAME --password=PASSWORD my.domain.com
